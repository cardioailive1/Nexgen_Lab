torch>=2.2.0
transformers>=4.43.0
peft>=0.11.0
bitsandbytes>=0.43.0
accelerate>=0.31.0
datasets>=2.19.0
pyyaml>=6.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0
pydantic>=2.7.0

# Experiment tracking — logs training runs to DagsHub's hosted MLflow.
# train_lora.py handles their absence gracefully (tracking just gets
# skipped with a printed warning), but include them for the real run.
mlflow>=2.10.0
dagshub>=0.3.0
